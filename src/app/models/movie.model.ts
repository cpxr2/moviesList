export class Movie{
    title: string;
    originalTitle: string;

    constructor(title: string, originalTitle: string){
        this.title = title;
        this.originalTitle = originalTitle;
    }

}